import React from 'react';

export default function EventCalendar() {
  return <div className="component border p-4 rounded-lg shadow-sm bg-white dark:bg-gray-800">EventCalendar TODO</div>;
}

// TODO: convertire a Tailwind + token
// Props: events (array), onEventClick (function), view ('month' | 'week' | 'day')
// Features: wrapper full-calendar, eventi colorati, drag&drop, responsive