import React from 'react';

export default function FeeStatusBadge() {
  return <div className="component inline-flex items-center px-2 py-1 rounded-full text-xs font-medium">FeeStatusBadge TODO</div>;
}

// TODO: convertire a Tailwind + token
// Props: status ('paid' | 'pending' | 'overdue'), amount (number), dueDate (string)
// Features: colori semantici, tooltip dettagli, animazioni