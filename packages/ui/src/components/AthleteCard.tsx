import React from 'react';

export default function AthleteCard() {
  return <div className="component border p-4 rounded-lg shadow-sm bg-white dark:bg-gray-800">AthleteCard TODO</div>;
}

// TODO: convertire a Tailwind + token
// Props: athlete (object), onClick (function), className (string)
// Features: foto atleta, nome, età, posizione, rating, badge stato