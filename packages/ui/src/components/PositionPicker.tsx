import React from 'react';

export default function PositionPicker() {
  return <div className="component border p-4 rounded-lg shadow-sm bg-white dark:bg-gray-800">PositionPicker TODO</div>;
}

// TODO: convertire a Tailwind + token
// Props: value (string), onChange (function), positions (array), disabled (boolean)
// Features: select ruoli (GK, CB, CM...), icone posizioni, validazione